#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  for (int a=97; a<=122; a++)
    putchar(a);
  for (int i=65; i<=90; i++)
    putchar(i);
  putchar('\n');
return (0);
}
